GXB v0.8.0 canonical multi-user state is created here at runtime.

On first launch, data/player_db.json is imported as the fixed anonymous sandbox player.
Credential accounts/sessions/player files and ownership indexes are then persisted here.
Do not place passwords or hand-written session tokens in this README.
